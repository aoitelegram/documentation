# $getOsMachine

This function retrieves the machine hardware name of the operating system using the os module.

## Example

```js
client.command({
  name: "misc",
  code: `
    $getOsMachine
  `,
});
```
