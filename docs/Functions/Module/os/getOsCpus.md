# $getOSTCpus

This function retrieves information about the CPU cores on the operating system using the os module.

## Example

```js
client.command({
  name: "misc",
  code: `
    $getOSTCpus
  `,
});
```
