# $clientCanReadAllGroupMessages

## Usage

if privacy mode is disabled for the bot

## Example

```javascript
<AoiClient>.addCommand({
  name: "command",
  code: `$replyMessage[$clientCanReadAllGroupMessages]`,
});
```
