# $clientUsername

## Usage

Username, for private chats, supergroups and channels if available

## Example

```javascript
client.command({
  name: "command",
  code: `$replyMessage[$clientUsername]`,
});
```
