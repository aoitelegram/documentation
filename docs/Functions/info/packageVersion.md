# $packageVersion

## Usage

Return `aoitelegram` version

## Example

```javascript
<AoiClient>.addCommand({
  name: "version",
  code: `$replyMessage[AoiTelegram version: $packageVersion]`,
});
```
